services:
  - type: web
    name: aegisai-ids
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run frontend/Home.py --server.port=10000 --server.address=0.0.0.0
    plan: free